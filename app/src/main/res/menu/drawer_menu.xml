<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/home"
            android:title="Home"
            android:icon="@drawable/ic_action_home"
            />
        <item
            android:id="@+id/consecionárias"
            android:title="Consecionárias"
            android:icon="@drawable/ic_action_conse"
            />
        <item
            android:id="@+id/revisões"
            android:title="Revisões"
            android:icon="@drawable/ic_revision"
            />
    </group>

    <item android:title="Controls">
        <menu>
            <item
                android:id="@+id/setting"
                android:title="Configurações"
                android:icon="@drawable/ic_action_setting"
                />
            <item
                android:id="@+id/logout"
                android:title="Logout"
                android:icon="@drawable/ic_action_logout"
                />
        </menu>
    </item>

</menu>